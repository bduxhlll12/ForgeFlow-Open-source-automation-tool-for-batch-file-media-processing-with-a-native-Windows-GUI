# FAQ

**Q: Where are logs stored?**
A: In the `logs/` folder next to the executable.

**Q: How do I add a preset?**
A: Drop a JSON file into `presets/`, see `presets/default.json` for the schema.
