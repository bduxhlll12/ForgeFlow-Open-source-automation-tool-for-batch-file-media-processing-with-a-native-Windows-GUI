# Architecture

- core/ — settings, logging, task queue, plugin API, presets
- gui/ — PySide6 widgets: main window, drag & drop, progress panel
- processing/ — file, video, image operations
- presets/ — JSON workflow presets
